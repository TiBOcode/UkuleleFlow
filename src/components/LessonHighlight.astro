---
---
<section class="lesson-highlight">
  <div class="container">
    <div class="content">
      <h2>One-on-One Ukulele Flow Session</h2>
      <div class="features">
        <div class="feature">
          <span class="icon">🎵</span>
          <p>Personalized Guidance Tailored to your Level</p>
        </div>
        <div class="feature">
          <span class="icon">⏰</span>
          <p>Full 1 Hour Immersive Session</p>
        </div>
        <div class="feature">
          <span class="icon">🌱</span>
          <p>Kickstart your Path to Creative Freedom</p>
        </div>
      </div>
      <div class="price">
        <span class="amount">€30</span>
        <span class="duration">/ 1 Hour Session</span>
      </div>
      <a href="/contact" class="cta-button">Book Your Lesson Now<br> 
        <em style="font-size: 0.8em;">33% off until January 31th</em>
      </a>
    </div>
  </div>
</section>

<style>
  .lesson-highlight {
    padding: 3rem 0; /* Reduced from 4rem */
    background: linear-gradient(135deg, rgba(255, 87, 51, 0.1) 0%, rgba(0, 0, 0, 0) 100%);
  }

  .content {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }

  h2 {
    font-size: 2.5rem;
    margin-bottom: 1.5rem; /* Reduced from 2rem */
    background: var(--orange-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    
  }

  .features {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem; /* Reduced from 2rem */
    margin: 1.5rem 0; /* Reduced from 2rem */
  }

  .feature {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem; /* Reduced from 1rem */
  }

  .icon {
    font-size: 1.75rem; /* Reduced from 2rem */
  }

  .feature p {
    color: var(--text-secondary);
    font-size: 1rem; /* Reduced from 1.1rem */
  }

  .price {
    margin: 1.5rem 0; /* Reduced from 2rem */
  }

  .amount {
    font-size: 2.75rem; /* Reduced from 3rem */
    font-weight: 700;
    color: #FF5733;
  }

  .duration {
    color: var(--text-secondary);
    font-size: 1.1rem; /* Reduced from 1.2rem */
  }

  .cta-button {
    display: inline-block;
    background: var(--orange-gradient);
    color: white;
    padding: 0.75rem 1.5rem; /* Reduced from 1rem 2rem */
    border-radius: 2rem;
    font-size: 1.1rem; /* Reduced from 1.2rem */
    font-weight: 600;
    text-decoration: none;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .cta-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(255, 87, 51, 0.25);
  }

  @media (max-width: 768px) {
    .features {
      grid-template-columns: 1fr;
      gap: 1rem; /* Further reduced for mobile */
    }

    .feature {
      gap: 0.25rem; /* Further reduced for mobile */
    }

    h2 {
      font-size: 2rem;
      text-align: left;
    }

    .amount {
      font-size: 2.5rem;
    }

    .icon {
      font-size: 1.5rem; /* Further reduced for mobile */
    }

    .feature p {
      font-size: 0.95rem;
    }
  }
</style>