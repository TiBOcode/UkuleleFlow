---
import SocialLinks from './SocialLinks.astro';
---
<section class="hero">
  <div class="container">
    <div class="profile">
      <div class="profile-image">
        <img src="/ProfilUkulele.jpeg" alt="TiBO" />
      </div>
    </div>
    <div class="bio">
      <h2>"Don’t Just Play, Create."</h2>
      <p>
        Because Music isn't just about playing songs or mastering techniques,  embracing music as a medium for self-expression, exploration, and joy.
      </p>
      <SocialLinks />
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 4rem 0;
  }

  .hero .container {
    display: grid;
    grid-template-columns: 40% 60%;
    gap: 2rem;
    align-items: center;
  }

  .profile-image {
    width: 250px;
    height: 250px;
    border-radius: 50%;
    overflow: hidden;
    margin: 0 auto;
    transition: transform 0.3s ease;
  }

  .profile-image:hover {
    transform: scale(1.05);
  }

  .profile-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .bio h2 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    background: var(--orange-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .bio p {
    font-size: 1.1rem;
    color: var(--text-secondary);
    max-width: 600px;
  }

  @media (max-width: 768px) {
    .hero .container {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .profile-image {
      width: 200px;
      height: 200px;
    }

    .bio {
      padding: 1rem;
    }
  }
</style>