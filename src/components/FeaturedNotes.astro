---
const steps = [
  {
    title: "Start with a Simple Structure",
    description: "Begin with basic chords & fingerpicking to create the foundation of your music. Build confidence and create a solid structure for your improvisation.",
    emoji: "🏛️",
    slug: "simple-structures",
    step: 1
  },
  {
    title: "Tune Into Your Senses",
    description: "Pay attention to the feel of the strings, the sound of each note, and the rhythm of your playing. Connect with the music on a deeper level through mindfulness.",
    emoji: "🧘",
    slug: "engage-all-the-senses",
    step: 2
  },
  {
    title: "Explore & Flow",
    description: "Let your music evolve organically. Experiment with dynamics, rhythms, and melodies until the Flow hits. Learn to dance with uncertainty and master the art of letting go.",
    emoji: "🌟",
    slug: "flow-state-in-music",
    step: 3
  }
];
---
<section class="featured-notes">
  <div class="container">
    <div class="process-grid">
      {steps.map(({ title, description, emoji, slug, step }) => (
        <div class="note-card">
          <div class="note-header">
            <div class="step-badge">
              <span class="emoji" style="filter: grayscale(1) brightness(2);">{emoji}</span>
              <span class="step-number">Step {step}</span>
            </div>
          </div>
          <h3>{title}</h3>
          <p>{description}</p>
          <a href={`/notes/${slug}`} class="read-more">
            Read More <span class="arrow">→</span>
          </a>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .featured-notes {
    padding: 0.5rem 0;
    background: linear-gradient(to bottom, transparent, rgba(0, 0, 0, 0.3));
  }

  .process-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  .note-card {
    background: rgba(255, 255, 255, 0.03);
    padding: 1.5rem;
    border-radius: 1rem;
    transition: transform 0.3s ease;
    display: flex;
    flex-direction: column;
  }

  .note-header {
    margin-bottom: 1rem;
  }

  .step-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--orange-gradient);
    padding: 0.5rem 1rem;
    border-radius: 2rem;
  }

  .emoji {
    font-size: 1.25rem;
  }

  .step-number {
    color: white;
    font-weight: 600;
    font-size: 0.85rem;
  }

  h3 {
    font-size: 1.25rem;
    margin-bottom: 0.75rem;
    color: var(--text-primary);
  }

  p {
    color: var(--text-secondary);
    line-height: 1.5;
    margin-bottom: 1.25rem;
    font-size: 0.95rem;
  }

  .read-more {
    margin-top: auto;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: #FF5733;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.9rem;
    transition: gap 0.2s ease;
  }

  .arrow {
    transition: transform 0.2s ease;
  }

  .read-more:hover {
    gap: 0.75rem;
  }

  .read-more:hover .arrow {
    transform: translateX(2px);
  }

  .note-card:hover {
    transform: translateY(-4px);
  }

  @media (max-width: 1024px) {
    .process-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .process-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .note-card {
      padding: 1.25rem;
    }

    h3 {
      font-size: 1.1rem;
    }

    p {
      font-size: 0.9rem;
    }
  }
</style>